# TEMPLATE ONLY - do not commit real values. Create the real secret out-of-band:
#   kubectl -n inference create secret generic vllm-api-key \
#     --from-literal=api-key="$(openssl rand -base64 32)"
apiVersion: v1
kind: Secret
metadata:
  name: vllm-api-key
  namespace: inference
type: Opaque
stringData:
  api-key: "<GENERATE_A_STRONG_RANDOM_KEY>"
